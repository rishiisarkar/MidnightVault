# midnight_vault does not require environment variables for the default browser wallet flow.
# Keep real local values in .env.local. Never commit mnemonics, seed phrases, or private keys.

NEXT_PUBLIC_MIDNIGHT_NETWORK=preprod
NEXT_PUBLIC_PREPROD_CONTRACT_ADDRESS=
NEXT_PUBLIC_LIVE_DEMO_URL=
NEXT_PUBLIC_DEMO_VIDEO_URL=
